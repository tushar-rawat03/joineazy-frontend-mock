# Joineazy Frontend - Round 2

This project is a React + Vite + Tailwind mock frontend for the Joineazy Round-2 task.

Run:

```
npm install
npm run dev
```

Login with `prof@example.com` (professor) or `stud1@example.com` (student).
